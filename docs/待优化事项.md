# 待优化事项（备选清单，暂未实施）

> 这里记的是"想做但先不做"的改进项。每条都写了**现状 / 怎么做 / 成本**，随时可以挑一条开工。
> 整理日期：2026-09-19

---

## 1. banner 顶部大图

- **现状**：`src/config.ts` 里 `banner.src` 指向 GitHub 头像 URL（`https://github.com/xiaochen56076.png`），当大图用会显得拉伸、像素偏低。
- **怎么做**：
  1. 准备一张比例约 **2.6 : 1 ~ 2.8 : 1** 的图（例如 2400×900），放到 `public/images/banner.webp`
  2. 把 `banner.src` 改成 `"/images/banner.webp"`
  3. 位置微调：`banner.position`（`center` / `top` / `bottom`）
  4. 高度微调：`src/constants/constants.ts` 里的 `BANNER_HEIGHT` / `BANNER_HEIGHT_EXTEND`（单位 vh）
- **成本**：5 分钟（**需要你提供图片**）

## 2. Footer 里可见英文的汉化

- **现状**：`src/components/Footer.astro` 会显示 `Powered by Astro & Fuwari`、`RSS`、`Sitemap`（这三个是可见文字，其余如 aria-label 不可见）。
- **怎么做**：直接把文字换成中文，例如「由 Astro 与 Fuwari 驱动」「订阅」「站点地图」；若以后要支持多语言，则往 `src/i18n/i18nKey.ts` 与 `src/i18n/languages/*.ts` 加键，再用 `i18n(...)` 取。
- **成本**：10 分钟

## 3. 音乐播放器换成自己的音乐

- **现状**：`src/config.ts` 的 `musicConfig.tracks` 是 3 首 SoundHelix 示例曲（外链）。
- **怎么做**：把音频放到对象存储或自建服务器，改 `tracks[].src`；`cover` 可留空。
- **注意**：跨域音频需要服务端允许 CORS；建议 mp3 / m4a。
- **成本**：取决于音频存放方式

## 4. 评论 / 访问统计

- **可选方案**：
  - **giscus**（基于 GitHub Discussions，无需自建后端，最省事）
  - Waline / Twikoo（需要部署一个服务端）
- **成本**：giscus 约 15 分钟

## 5. 后台入口对访客的可见性

- **现状**：导航栏的齿轮「后台设置」**所有访客都能看到**（点进去只会看到登录页，没有账号也进不去）。
- **可选做法**：
  - 保持现状（反正进不去）
  - 或只在 URL 带参数时显示（例如 `?admin=1`），平时隐藏
  - 或干脆去掉导航栏入口，只用 `/admin/` 直达
- **成本**：10 分钟

## 6. 其它小项

| 项目 | 位置 | 说明 |
|---|---|---|
| favicon | `public/favicon/` | 换成自己的图标 |
| 每页文章数 | `src/constants/constants.ts` → `PAGE_SIZE` | 当前 8 |
| 页面宽度 | 同上 → `PAGE_WIDTH` | 当前 75rem |
| 默认主题 | 同上 → `DEFAULT_THEME` | 亮 / 暗 / 跟随系统 |
| 个人简介与社交链接 | `src/config.ts` → `profileConfig` | 现在是"这里是一段个人简介。"与 X/QQ/GitHub 三个链接 |
| 站点标题/副标题 | `src/config.ts` → `siteConfig` | 影响首页与浏览器标签 |
| 关于页内容 | `src/content/pages/about.md` 或后台「页面」集合 | |

---

## 已完成（留档，便于回看做过什么）

- [x] 界面与配置全面汉化（见 `docs/汉化与配置说明.md`）
- [x] 头像、导航栏 GitHub 链接指向自己的账号
- [x] 后台（Sveltia CMS）+ GitHub 授权登录 + 用户名白名单（见 `docs/后台使用说明.md`）
- [x] 导航栏齿轮入口「后台设置」+ 浮层互斥修复
- [x] 示例文章撤下并归档（`docs/示例文章备份/`）
- [x] 侧栏「文章 / 分类 / 标签」统计，三项可点击
- [x] 新增 `/tags/`、`/categories/` 总览页
- [x] 右下角「回到顶部」按钮
- [x] 侧栏音乐播放器（见 `docs/音乐播放器说明.md`）
- [x] 真实域名 `site:` 与全站链接（`https://56076077.xyz/`）
